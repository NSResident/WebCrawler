Requester:
    Implement GET
        -Error checks socket connections
        -Chunking (Might have to ask)
    Implement POST (HOLD)
    SSL OPTIONAL***
        -WireShark for 300 Error

Crawling:

Parsing:

BruteForce:
    Find form elements
    Notify user whether a login was succesful 

Questions:
    -Do the keywords have to be in search order

